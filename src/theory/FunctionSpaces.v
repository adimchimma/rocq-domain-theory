(** * FunctionSpaces

    Phase 0: Exponential (function space) CPOs.

    Definitions to be added:
    - Continuous function space [D ->c E] with pointwise order.
    - CPO structure: lub of a chain of continuous functions is pointwise lub.
    - Application [eval : (D ->c E) * D ->c E] is continuous.
    - Currying and uncurrying preserve continuity.
    - This is the key construction for interpreting function types in PCF.
*)
