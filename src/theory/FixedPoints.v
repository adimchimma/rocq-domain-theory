(** * FixedPoints

    Phase 0: Kleene fixed-point theorem.

    Definitions and lemmas to be added:
    - Iteration of a continuous endofunction from bottom.
    - The Kleene chain [bot, f(bot), f(f(bot)), ...] is an omega-chain.
    - The least fixed point [fix f] is the join of the Kleene chain.
    - [fix_unfold]: [fix f = f (fix f)].
    - [fix_least]: [f x <= x -> fix f <= x].
    - Fixed-point induction ([fix_ind]).
*)
