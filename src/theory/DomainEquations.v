(** * DomainEquations

    Phase 0 / Phase 1: Inverse limits and recursive domain equations.

    Definitions and lemmas to be added:

    Phase 0 (plain CPO setting):
    - Embedding-projection pairs ([ep_pair]) and their composition laws.
    - Mixed-variance bifunctors ([bifunctor]) on CPOs.
    - Inverse-limit CPO as the limit of an [ep]-sequence.
    - Embedding and projection maps into and out of the inverse limit.
    - Universal property of the inverse limit.

    Phase 1 (enriched setting):
    - Lifting the inverse-limit construction to CPO-enriched categories.
    - Derivation of the canonical domain equation [D ~= [D ->c D]_bot].
*)
