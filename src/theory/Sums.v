(** * Sums

    Phase 0: Sum (coproduct) CPOs.

    Definitions to be added:
    - Disjoint union [D + E] with the flat coproduct order.
    - CPO structure and lub computation for chains inside each summand.
    - Injections [inl] and [inr] are continuous.
    - Case analysis is continuous when both branches are.
*)
